# ретворення тексту в верхній регістр.
# //Видалення пробілів на початку та в кінці рядка.


def to_uppercase(text: str) -> str:
    return text.upper()


def strip_text(text: str) -> str:
    return text.strip()
